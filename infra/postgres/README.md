# Postgres

Butr's durable state should eventually live in Postgres.

Planned tables:

- `trade_previews`
- `approval_requests`
- `execution_results`
- `audit_events`
- `portfolio_snapshots`
- `broker_accounts`

